# SPDX-License-Identifier: GPL-3.0-or-later
# Phrases (case-insensitive, tokenized like names) that LEGAL.BRAND must not
# flag even though they contain a blocklisted mark. A mark is exempt only
# where it sits inside an allowlisted phrase, so "tesla_coil" passes while
# "tesla_coil_tesla_logo" still fails. Every entry needs a comment saying why.

tesla coil      # the electrical device (Nikola Tesla), a common prop
nikola tesla    # the inventor, not the car maker
